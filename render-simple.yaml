services:
  - type: web
    name: deposafety-api
    runtime: python
    plan: free
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn app_simple:app --host 0.0.0.0 --port $PORT
    rootDir: backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
